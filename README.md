# AI Noter

Десктоп-приложение для создания и управления заметками с интегрированной поддержкой искусственного интеллекта.

## 📋 Описание

AI Noter — это современное desktop-приложение, разработанное на Python с использованием фреймворка Qt. Приложение позволяет создавать и управлять заметками, организовывать напоминания и использовать возможности искусственного интеллекта для работы с текстом.

## ✨ Возможности

- 📝 **Управление заметками**
  - Создание, редактирование и удаление заметок
  - Поддержка текста и изображений
  - Автоматическое сохранение контента
  
- ⏰ **Система напоминаний**
  - Создание напоминаний с указанием даты и времени
  - Автоматические уведомления со звуковым сигналом
  - Фоновая проверка активных напоминаний

- 🤖 **Интеграция с ИИ**
  - Запросы к DeepSeek API для обработки текста
  - История всех запросов
  - Сохранение ответов
  - Удобное добавление API ключа прямо из интерфейса приложения

- 🔐 **Безопасность**
  - Система авторизации пользователей
  - Изолированное хранение данных для каждого пользователя
  - Локальная база данных SQLite
  
- 🎨 **Удобный интерфейс**
  - Интуитивная навигация с кнопками возврата
  - Современный дизайн интерфейса
  - Удобное управление всеми функциями

## 🛠 Технологии

- **Python 3** — основной язык программирования
- **PyQt6** — фреймворк для создания графического интерфейса
- **SQLite** — база данных для хранения данных
- **DeepSeek API** — интеграция с искусственным интеллектом
- **python-dotenv** — управление переменными окружения

## 📦 Установка

### Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонируйте репозиторий или скачайте проект:**
   ```bash
   git clone <repository-url>
   cd ai_noter
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройка API ключа DeepSeek:**
   
   Вы можете добавить API ключ двумя способами:
   
   **Способ 1: Через интерфейс приложения (рекомендуется)**
   - После входа в систему в главном меню появится кнопка "🔑 Добавить API ключ DeepSeek" (если ключ ещё не добавлен)
   - Нажмите на кнопку и введите ваш API ключ в появившемся диалоговом окне
   - Ключ будет автоматически сохранён в файл `.env`
   
   **Способ 2: Вручную через файл .env**
   - Создайте файл `.env` в корне проекта (используйте `env.example` как шаблон):
     ```bash
     cp env.example .env
     ```
   - Откройте `.env` и добавьте ваш API ключ DeepSeek:
     ```
     DEEPSEEK_API_KEY=your_api_key_here
     ```
   
   > Для получения API ключа зарегистрируйтесь на [DeepSeek](https://www.deepseek.com/) или можете обратиться в Telegram [@coawy](https://t.me/coawy/) для получения API-ключа для тестирования проекта

4. **Запустите приложение:**
   ```bash
   python main.py
   ```

## 🚀 Использование

### Первый запуск

1. При первом запуске приложение автоматически создаст базу данных `ai_noter.db`
2. Зарегистрируйте нового пользователя, нажав кнопку "Регистрация"
3. Войдите в систему используя ваши учетные данные
4. Если вы ещё не добавили API ключ DeepSeek, в главном меню появится кнопка "🔑 Добавить API ключ DeepSeek" — нажмите на неё и введите ваш ключ для активации функций ИИ

### Основные функции

#### Работа с заметками

1. В главном меню выберите "Заметки"
2. Нажмите "Создать новую заметку" для создания новой заметки
3. Используйте кнопки "Добавить текст" и "Добавить фото" для расширенного редактирования
4. Нажмите "Сохранить" для сохранения заметки
5. Для возврата в главное меню используйте кнопку "←" в левом верхнем углу

#### Напоминания

1. В главном меню выберите "Напоминания"
2. Создайте новое напоминание с указанием текста, даты и времени
3. При наступлении времени напоминания появится уведомление со звуковым сигналом
4. Для возврата в главное меню используйте кнопку "←" в левом верхнем углу

#### Работа с ИИ

1. В главном меню выберите "Искусственный интеллект"
2. Создайте новый запрос или откройте существующий из истории
3. Введите ваш запрос и нажмите "Отправить"
4. Ответ от ИИ появится в поле ниже
5. Для возврата в главное меню используйте кнопку "←" в левом верхнем углу
6. **Важно:** Для работы функций ИИ необходим активный API ключ DeepSeek. Если ключ не добавлен, в главном меню будет отображаться кнопка для его добавления

## 📁 Структура проекта

```
ai_noter/
├── main.py                 # Точка входа приложения
├── db.py                   # Модуль работы с базой данных
├── models.py              # Модели данных (User, Note, Reminder, AIRequest)
├── paths.py               # Утилита для определения путей к ресурсам
├── reminder_watcher.py     # Фоновая обработка напоминаний
├── requirements.txt        # Зависимости проекта
├── env.example            # Пример файла с переменными окружения
├── alarm.wav              # Звуковой файл для напоминаний
├── ui/                    # Модули пользовательского интерфейса
│   ├── login.py           # Окно авторизации
│   ├── main_menu.py       # Главное меню
│   ├── notes_list.py      # Список заметок
│   ├── note_editor.py     # Редактор заметок
│   ├── reminders_list.py  # Список напоминаний
│   ├── reminder_editor.py # Редактор напоминаний
│   ├── ai_list.py         # Список ИИ-запросов
│   └── ai_request.py      # Окно работы с ИИ
└── storage/               # Хранилище данных
    └── images/            # Изображения из заметок
```

## 🔧 Настройка

### Конфигурация базы данных

База данных SQLite создается автоматически при первом запуске. Файл `ai_noter.db` будет создан в корне проекта.

### Настройка звука напоминаний

По умолчанию используется файл `alarm.wav`. Вы можете заменить его на свой звуковой файл, изменив параметр `sound_file` в `reminder_watcher.py` или поместив свой файл с именем `alarm.wav` в корень проекта.

## ⚠️ Решение проблем

### Ошибка "DEEPSEEK_API_KEY not found"

Убедитесь, что:
- Файл `.env` существует в корне проекта (создаётся автоматически при добавлении ключа через интерфейс)
- В файле `.env` указан правильный API ключ
- Формат файла `.env` правильный: `DEEPSEEK_API_KEY=your_key`
- Если вы добавили ключ через интерфейс, перезапустите приложение для применения изменений

**Совет:** Самый простой способ добавить ключ — использовать кнопку "🔑 Добавить API ключ DeepSeek" в главном меню приложения.

### Ошибки TSMSendMessageToUIServer (macOS)

Эти предупреждения не критичны и не влияют на работу приложения. Это известная особенность macOS при работе с PyQt6.

## 📝 Лицензия

Этот проект предназначен для образовательных и личных целей.

## 👤 Автор

Разработано Mikael Zakharyan

## 🔮 Планы развития

- [ ] Расширение функциональности ИИ
- [ ] Внедрение системы тегов и категорий для заметок
- [ ] Экспорт заметок в различные форматы

---

**Примечание:** Для работы функций ИИ необходим активный API ключ DeepSeek. Без него приложение будет работать, но функции ИИ будут недоступны. Вы можете обратиться в Telegram [@coawy](https://t.me/coawy/) для получения API-ключа для тестирования проекта.
